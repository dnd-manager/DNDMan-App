import 'package:flutter/material.dart';

class MobileNavbar extends StatelessWidget {
  final List<Widget> buttons;

  const MobileNavbar({Key? key, required this.buttons}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
